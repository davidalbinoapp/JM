# Bibliotecas Python que o Jornal Mural precisa.
# Instaladas automaticamente pelos instaladores (setup_mac.command /
# setup_windows.bat). Sem versão travada de propósito: assim funciona em
# qualquer versão de Python (3.10 em diante), no Mac e no Windows.
python-pptx
python-docx
Pillow
pymupdf
# numpy: usado no enquadramento pelo ASSUNTO (saliência por resíduo espectral).
# Sem ele, o motor ainda roda, mas o corte cai pro centro (a saliência é pulada).
# Antes vinha "de carona" só quando o OpenCV (opcional) era instalado — agora é
# explícito pra o enquadramento funcionar mesmo numa instalação sem OpenCV.
numpy
